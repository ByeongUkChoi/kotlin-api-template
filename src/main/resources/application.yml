spring:
  datasource:
    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
#  data.sql 쓰기위해
#  jpa:
#    defer-datasource-initialization: true
